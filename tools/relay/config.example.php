<?php
/**
 * Настройки приёмника заявок.
 *
 * Скопируйте этот файл рядом под именем config.php и впишите свои значения.
 * config.php лежит на сервере и в браузер не отдаётся — токен из него
 * посетитель сайта увидеть не может.
 */

return [
    // Токен бота MAX. Выдаётся на business.max.ru/self после модерации бота
    // (модерация занимает до 48 часов). Пока токена нет — оставьте пустым,
    // заявки будут приходить только на почту.
    'max_token' => '',

    // Кому бот пишет заявку. Это числовой идентификатор пользователя MAX,
    // а не номер телефона. Узнаётся так: напишите своему боту любое сообщение
    // и посмотрите обновления методом GET /updates — там будет ваш user_id.
    'max_user_id' => '',

    // Почта, на которую дублируются заявки. Работает сразу, без модерации.
    'mail_to' => 'asia.nsk@mail.ru',

    // Обратный адрес письма. Должен быть на том же домене, что и сайт, —
    // иначе почтовые службы отправят письмо в спам.
    'mail_from' => 'no-reply@asia-reika.ru',
];
